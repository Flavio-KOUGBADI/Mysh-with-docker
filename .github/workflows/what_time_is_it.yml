name: Demo 1
run-name: display date
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "================================"
      - run: ls -la
      - run: echo "================================"
